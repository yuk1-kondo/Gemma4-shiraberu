# iOS Migration Workspace

- Main iOS scaffold lives in `XEye/`
- Open `ios/XEye/Package.swift` with Xcode to inspect the Swift modules
- Android source remains the reference implementation in `app/`
